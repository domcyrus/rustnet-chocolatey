name: Update Chocolatey Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'RustNet version to update to (e.g., v0.18.0 or 0.18.0)'
        required: true
        type: string
      publish:
        description: 'Publish to Chocolatey Community Repository'
        required: false
        type: boolean
        default: true

jobs:
  update-package:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Normalize version input
        id: version
        shell: pwsh
        run: |
          # Strip 'v' prefix if provided (e.g., v0.18.0 -> 0.18.0)
          $version = "${{ inputs.version }}" -replace '^v', ''
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "tag=v$version" >> $env:GITHUB_OUTPUT
          Write-Host "Target version: v$version"

      - name: Verify release is published and stable
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ steps.version.outputs.tag }}"

          # Check the release exists and is not a draft
          $release = gh release view $tag --repo domcyrus/rustnet --json isDraft,tagName 2>&1
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Release $tag not found in domcyrus/rustnet"
            exit 1
          }
          $releaseData = $release | ConvertFrom-Json
          if ($releaseData.isDraft) {
            Write-Error "Release $tag is still a draft, assets may change. Wait for the release workflow to finish."
            exit 1
          }

          Write-Host "Release $tag is published."

      - name: Get current package version
        id: current
        shell: pwsh
        run: |
          $currentVersion = ([xml](Get-Content rustnet.nuspec)).package.metadata.version
          echo "version=$currentVersion" >> $env:GITHUB_OUTPUT
          Write-Host "Current package version: v$currentVersion"

      - name: Check if update is needed
        id: check
        shell: pwsh
        run: |
          # Always run: even if version matches, checksums may be stale
          echo "needs_update=true" >> $env:GITHUB_OUTPUT
          $current = "${{ steps.current.outputs.version }}"
          $target = "${{ steps.version.outputs.version }}"
          if ($current -eq $target) {
            Write-Host "Same version (v$current) — will verify checksum is correct"
          } else {
            Write-Host "Update needed: v$current -> v$target"
          }

      - name: Download binary and calculate checksum
        if: steps.check.outputs.needs_update == 'true'
        id: checksum
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $url = "https://github.com/domcyrus/rustnet/releases/download/v$version/rustnet-v$version-x86_64-pc-windows-msvc.zip"

          Write-Host "Downloading from: $url"
          Invoke-WebRequest -Uri $url -OutFile "rustnet.zip"

          $checksum = (Get-FileHash -Algorithm SHA256 rustnet.zip).Hash.ToLower()
          echo "checksum=$checksum" >> $env:GITHUB_OUTPUT
          Write-Host "Calculated checksum: $checksum"

          # Clean up
          Remove-Item rustnet.zip

      - name: Update package files
        if: steps.check.outputs.needs_update == 'true'
        shell: pwsh
        env:
          CURRENT_VERSION: ${{ steps.current.outputs.version }}
          NEW_VERSION: ${{ steps.version.outputs.version }}
          CHECKSUM: ${{ steps.checksum.outputs.checksum }}
        run: |
          Write-Host "Updating package with:"
          Write-Host "  Version: v$env:CURRENT_VERSION -> v$env:NEW_VERSION"
          Write-Host "  Checksum: $env:CHECKSUM"

          # Update nuspec version
          $nuspecPath = "rustnet.nuspec"
          $nuspecContent = Get-Content $nuspecPath -Raw
          $nuspecContent = $nuspecContent -replace '<version>[\d\.]+</version>', "<version>$env:NEW_VERSION</version>"
          Set-Content -Path $nuspecPath -Value $nuspecContent -NoNewline

          # Update chocolateyinstall.ps1 URL and checksum
          $installScriptPath = "tools\chocolateyinstall.ps1"
          $installContent = Get-Content $installScriptPath -Raw
          $installContent = $installContent -replace 'releases/download/v[\d\.]+/rustnet-v[\d\.]+-', "releases/download/v$env:NEW_VERSION/rustnet-v$env:NEW_VERSION-"
          $installContent = $installContent -replace "checksum64 = '[^']*'", "checksum64 = '$env:CHECKSUM'"
          Set-Content -Path $installScriptPath -Value $installContent -NoNewline

          Write-Host "Package files updated successfully"
          Write-Host ""
          Write-Host "Changes:"
          git diff

      - name: Verify checksum matches release asset
        if: steps.check.outputs.needs_update == 'true'
        id: verify
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $expected = "${{ steps.checksum.outputs.checksum }}"
          $url = "https://github.com/domcyrus/rustnet/releases/download/v$version/rustnet-v$version-x86_64-pc-windows-msvc.zip"

          Write-Host "Re-downloading to verify checksum is stable..."
          Invoke-WebRequest -Uri $url -OutFile "verify.zip"
          $actual = (Get-FileHash -Algorithm SHA256 verify.zip).Hash.ToLower()
          Remove-Item verify.zip

          if ($actual -ne $expected) {
            Write-Error "Checksum mismatch: expected $expected, got $actual"
            Write-Error "Assets may have been replaced. Aborting to prevent pushing stale checksums."
            exit 1
          }
          Write-Host "Checksum verified: $actual"

      - name: Build package
        if: steps.check.outputs.needs_update == 'true'
        shell: pwsh
        run: |
          choco pack
          $nupkg = Get-ChildItem *.nupkg | Select-Object -First 1
          Write-Host "Built package: $($nupkg.Name)"

      - name: Test package installation (clean install)
        if: steps.check.outputs.needs_update == 'true'
        shell: pwsh
        run: |
          choco install rustnet -source "'.;https://community.chocolatey.org/api/v2/'" -y

          if (-not (Get-Command rustnet -ErrorAction SilentlyContinue)) {
            Write-Error "Package installation failed: rustnet not on PATH"
            exit 1
          }
          $exe = "C:\ProgramData\chocolatey\lib\rustnet\tools\rustnet.exe"
          if (-not (Test-Path $exe)) {
            Write-Error "Clean install: rustnet.exe missing at $exe"
            exit 1
          }
          Write-Host "Clean install OK (rustnet.exe present in tools/)"

          choco uninstall rustnet -y

      - name: Test package upgrade from previously-published version
        if: steps.check.outputs.needs_update == 'true'
        shell: pwsh
        run: |
          # Install the latest version currently on the community feed,
          # then upgrade to the locally-built package. This catches the
          # class of bug where leftover files from the previous install
          # confuse the new install script (regression for the
          # tools/-wipe bug fixed in 1.3.0.1).
          choco install rustnet --source 'https://community.chocolatey.org/api/v2/' -y
          if (-not (Test-Path 'C:\ProgramData\chocolatey\lib\rustnet\tools\rustnet.exe')) {
            Write-Error "Setup failure: previously-published rustnet did not install cleanly"
            exit 1
          }

          choco upgrade rustnet --source "'.;https://community.chocolatey.org/api/v2/'" -y --force

          $exe = "C:\ProgramData\chocolatey\lib\rustnet\tools\rustnet.exe"
          if (-not (Test-Path $exe)) {
            Write-Error "Upgrade install: rustnet.exe missing at $exe (regression of tools/-wipe bug)"
            exit 1
          }
          if (-not (Get-Command rustnet -ErrorAction SilentlyContinue)) {
            Write-Error "Upgrade install: rustnet not on PATH"
            exit 1
          }
          Write-Host "Upgrade install OK (rustnet.exe still present after upgrade)"

          choco uninstall rustnet -y

      - name: Commit and push changes
        if: steps.check.outputs.needs_update == 'true'
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add rustnet.nuspec tools/chocolateyinstall.ps1

          # Only push if there are actual changes
          $diff = git diff --cached --quiet; $hasChanges = $LASTEXITCODE -ne 0
          if ($hasChanges) {
            git commit -m "Update RustNet to version v$version"
            git push
          } else {
            Write-Host "No checksum changes needed — package already correct"
          }

      - name: Publish to Chocolatey
        if: steps.check.outputs.needs_update == 'true' && inputs.publish
        shell: pwsh
        env:
          CHOCO_API_KEY: ${{ secrets.CHOCO_API_KEY }}
        run: |
          if ([string]::IsNullOrEmpty($env:CHOCO_API_KEY)) {
            Write-Warning "CHOCO_API_KEY not set. Skipping publish."
            Write-Host "To enable publishing, add CHOCO_API_KEY to repository secrets."
            exit 0
          }

          $nupkg = Get-ChildItem *.nupkg | Select-Object -First 1
          Write-Host "Publishing $($nupkg.Name) to Chocolatey Community Repository..."

          choco apikey --key $env:CHOCO_API_KEY --source https://push.chocolatey.org/
          choco push $nupkg.Name --source https://push.chocolatey.org/

          Write-Host "Package published successfully!"
          Write-Host "Note: Moderator approval may take some time."

      - name: Create summary
        if: always()
        shell: pwsh
        run: |
          $current = "${{ steps.current.outputs.version }}"
          $target = "${{ steps.version.outputs.version }}"
          $needsUpdate = "${{ steps.check.outputs.needs_update }}"
          $checksum = "${{ steps.checksum.outputs.checksum }}"
          $publish = "${{ inputs.publish }}"

          @"
          ### Chocolatey Package Update

          - **Current Version:** v$current
          - **Target Version:** v$target

          "@ | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append

          if ($needsUpdate -eq "true") {
            @"
          **Package Updated Successfully!**

          - **New Version:** v$target
          - **SHA256 Checksum:** ``$checksum``
          - **Published to Chocolatey:** $(if ($publish -eq "true") { "Yes" } else { "No" })

          **Validation:**
          - Package built successfully
          - Installation tested on Windows
          "@ | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          } else {
            @"
          **No Update Needed**

          The package is already at the target version.
          "@ | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          }
